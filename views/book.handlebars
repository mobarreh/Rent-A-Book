<section class="card">
  <a href="/book/{{id}}">
    <header>{{book_name}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>Price: {{price}}</p>
    </div>
  </a>
</section>